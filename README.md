# React Rock Collection

Коллекция камней и минералов с 3D моделями. Проект состоит из React приложения и Express сервера.

## 🚀 Запуск проекта

### Предварительные требования

- Node.js (версия 16 или выше)
- npm

### Установка зависимостей

```bash
npm install
```

### Запуск сервера

Сервер предоставляет API для работы с данными камней и запускается на порту 3001.

```bash
npm run server
```

### Запуск React приложения

Приложение запускается на порту 3000 и автоматически проксирует запросы к серверу.

```bash
npm start
```

### Одновременный запуск

Для удобства можно запустить сервер и приложение в разных терминалах, или использовать инструменты вроде `concurrently`.

## 📱 Использование приложения

### Основная галерея

Главная страница (`/`) отображает коллекцию камней в виде карточек. Каждый камень содержит:

- Название минерала
- Описание
- Изображение
- Ссылку на подробный просмотр

### Просмотр камня

Страница `/stone/:id` показывает детальную информацию о выбранном камне.

## 🛠️ Админ-панель

### Доступ к админке

Админ-панель доступна по адресу `/admin` или через горячую клавишу:

- **Windows/Linux**: `Ctrl + Shift + A`
- **Mac**: `Cmd + Shift + A`

### Возможности админки

Админ-панель позволяет управлять коллекцией камней:

#### Просмотр всех камней

- Список всех камней с названиями и ссылками на изображения
- Счетчик общего количества камней

#### Добавление нового камня

1. Нажмите кнопку "Добавить" или заполните форму в правой части экрана
2. Заполните обязательные поля:
   - **Название** - название минерала
   - **Описание** - подробное описание камня
3. Опционально заполните:
   - **Ссылка на изображение** - путь к изображению камня
   - **Путь к 3D модели** - путь к GLB файлу модели
4. Нажмите "Добавить"

#### Редактирование камня

1. В списке камней нажмите "Редактировать" рядом с нужным камнем
2. Форма автоматически заполнится текущими данными
3. Внесите необходимые изменения
4. Нажмите "Сохранить" или "Отменить" для отмены изменений

### Валидация данных

- Название и описание являются обязательными полями
- При сохранении выполняется проверка на заполненность обязательных полей

## 🏗️ Структура проекта

```
react-rock/
├── server/                 # Express сервер
│   └── index.js           # API endpoints и логика сервера
├── src/                   # React приложение
│   ├── api/               # API клиенты
│   │   └── rocksApi.js    # Функции для работы с API камней
│   ├── components/        # React компоненты
│   │   ├── admin/         # Админ-панель
│   │   ├── card/          # Карточка камня
│   │   ├── home/          # Главная страница
│   │   └── view/          # Страница просмотра камня
│   └── App.jsx            # Основной компонент с роутингом
├── data/                  # Данные приложения
│   └── rocks.json         # JSON файл с коллекцией камней
└── public/                # Статические файлы
    └── images/            # Изображения камней
```

## 🔧 API Endpoints

Сервер предоставляет следующие REST API endpoints:

- `GET /api/rocks` - Получить все камни
- `GET /api/rocks/:id` - Получить камень по ID
- `POST /api/rocks` - Создать новый камень
- `PUT /api/rocks/:id` - Обновить камень
- `DELETE /api/rocks/:id` - Удалить камень

## 📊 Данные

Все данные о камнях хранятся в файле `data/rocks.json`. Каждый камень имеет следующую структуру:

```json
{
  "id": 1,
  "name": "Аметист",
  "description": "Фиолетовая разновидность кварца...",
  "image": "/images/amethyst.png",
  "modelPath": "/models/amethyst.glb"
}
```

## 🎨 Стилизация

Проект использует SCSS модули для стилизации компонентов. Каждый компонент имеет свой файл стилей в той же папке.

## 📦 Скрипты

- `npm start` - Запуск React приложения в режиме разработки
- `npm run server` - Запуск Express сервера
- `npm run build` - Сборка production версии React приложения
- `npm test` - Запуск тестов
- `npm run eject` - Извлечение конфигурации Create React App

## 🔐 Безопасность

Админ-панель не имеет аутентификации и доступна всем пользователям. В production окружении рекомендуется добавить механизмы аутентификации и авторизации.
